name: Release

on:
  release:
    types: [created]

permissions:
  contents: write

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        arch: [arm64, x86_64]
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Import certificate
        env:
          CERTIFICATE_BASE64: ${{ secrets.CERTIFICATE_BASE64 }}
          CERTIFICATE_PASSWORD: ${{ secrets.CERTIFICATE_PASSWORD }}
        run: |
          CERT_PATH=$RUNNER_TEMP/cert.p12
          KEYCHAIN_PATH=$RUNNER_TEMP/build.keychain-db
          echo -n "$CERTIFICATE_BASE64" | base64 --decode -o "$CERT_PATH"
          security create-keychain -p "" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "" "$KEYCHAIN_PATH"
          security import "$CERT_PATH" -P "$CERTIFICATE_PASSWORD" -A -t cert -f pkcs12 -k "$KEYCHAIN_PATH"
          security set-key-partition-list -S apple-tool:,apple: -k "" "$KEYCHAIN_PATH"
          security list-keychain -d user -s "$KEYCHAIN_PATH"

      - name: Build and bundle
        run: bash scripts/bundle.sh ${{ matrix.arch }}

      - name: Sign app
        run: |
          codesign --force --options runtime --sign "Developer ID Application" --deep "dist/Vitrail.app"
          codesign --verify --verbose "dist/Vitrail.app"

      - name: Notarize
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_ID_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}
          APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
        run: |
          # Notarize the app (submit as zip, staple the .app)
          ditto -c -k --keepParent "dist/Vitrail.app" "dist/Vitrail-notarize.zip"
          xcrun notarytool submit "dist/Vitrail-notarize.zip" \
            --apple-id "$APPLE_ID" \
            --password "$APPLE_ID_PASSWORD" \
            --team-id "$APPLE_TEAM_ID" \
            --wait
          xcrun stapler staple "dist/Vitrail.app"
          rm "dist/Vitrail-notarize.zip"

      - name: Create DMG with stapled app
        run: |
          rm -f "dist/Vitrail-${{ matrix.arch }}.dmg"
          mkdir -p dist/dmg
          cp -r dist/Vitrail.app dist/dmg/
          ln -s /Applications dist/dmg/Applications
          hdiutil create -volname "Vitrail" \
            -srcfolder dist/dmg \
            -ov -format UDZO \
            "dist/Vitrail-${{ matrix.arch }}.dmg"
          rm -rf dist/dmg

      - name: Upload to release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh release upload "${{ github.event.release.tag_name }}" "dist/Vitrail-${{ matrix.arch }}.dmg" --clobber
